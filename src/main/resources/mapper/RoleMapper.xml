<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.shoto.springboot.shiro.mapper.RoleMapper">

    <select id="getAllRoleListByUsername" resultType="com.shoto.springboot.shiro.entity.Role">
        SELECT t1.* FROM role t1 LEFT JOIN user_role t2 ON t1.id = t2.role_id
        LEFT JOIN user t3 ON t2.user_id = t3.id
        WHERE t3.username =#{username}
    </select>

</mapper>